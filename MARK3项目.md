# AI大促队长 W1-W2 开工任务清单
> 直接交给工程执行。每项带验收标准，做完打钩。每日 15 分钟站会对齐，卡壳当天上报。  
配套文档：《AI大促队长系统设计文档（POC版）v0.2》
>

---

## 开工前置（D0，半天内完成）
- [ ] [ ]

**申请内部 LLM 网关权限**（最大不确定性，今天就提，附用途说明）

- [ ] [ ]

确认 Mac mini 芯片/内存：≥16GB→兜底 Qwen2.5-14B(Q4)；8GB→7B 或纯网关

- [ ] [ ]

确认感知节点 MacBook（谁出机、系统版本、常驻位置）

- [ ] [ ]

系统设置：关闭休眠/屏保/自动更新；设置断电自启

- [ ] [ ]

下单：指向性 USB 麦克风（先到先用，内置麦先行开发）；备用 USB 摄像头

- [ ] [ ]

音频链路试通：Mac mini → 3.5mm/USB → 功放出声

---

## W1：会说话的大屏
### D1 环境与工程骨架
- [ ] [ ]

Python 3.11 + venv；`pip install fastapi uvicorn websockets apscheduler httpx pydantic`

- [ ] [ ]

建工程骨架（目录结构照设计文档 §6）：`orchestrator/ speech/ tools/ screens/ scripts/ assets/`

- [ ] [ ]

编排服务最小进程：`GET /health` 返回 ok；`WS /events` 可连接可广播

- [ ] [ ]

验收：浏览器 wscat 连上 /events，能收到心跳消息

### D2 事件总线 + 大屏接入
- [ ] [ ]

实现四种核心事件：`broadcast / metric_card / task_progress / mode`（schema 照设计文档 §3.2）

- [ ] [ ]

事件落盘：每条事件追加写 JSONL（事后回放审计用）

- [ ] [ ]

大屏改造：以 v7 HTML 为基线，删演示脚本，换 WS 客户端消费事件（渲染逻辑不动）

- [ ] [ ]

验收：`curl`/脚本向 WS 推一条 broadcast 事件 → 大屏字幕显示 + 状态变化

### D3 TTS 链路
- [ ] [ ]

`pip install sherpa-onnx`；下载 vits-melo-zh_en 模型（HF 或 modelscope 镜像）

- [ ] [ ]

`speech/tts.py`：text→wav；实现数字/单位读法归一化（"3.28亿"→"三点二八亿"，"99.95%"→"百分之九十九点九五"）

- [ ] [ ]

音色试听：melo 可用女声/男声全部过一遍，拉上存孝定版

- [ ] [ ]

台本预生成：开场白/固定播报走阿里云 TTS 出 wav 存 `scripts/audio/`

- [ ] [ ]

验收：broadcast 事件 → 本地 TTS 出声 + 大屏字幕同步滚动

### D4 定时调度 + 台本管理
- [ ] [ ]

`scripts/playbook.yaml`：时间→文本→（预生成音频 | 实时TTS）映射

- [ ] [ ]

APScheduler 挂载：整点播报、静默令、值班提醒（cron 可配）

- [ ] [ ]

验收：改系统时间 ±2 分钟，触发一条整点播报全链路出声

### D5 W1 里程碑
- [ ] [ ]

演示"它自己会说话"：2 条定时事件 + 1 条手动 API 触发，全链路真机跑

- [ ] [ ]

录屏存档，拿去给领导/同事看 5 分钟收反馈

- [ ] [ ]

**里程碑标准：大屏+声音+调度全自动，无人操作跑 10 分钟不崩**

---

## W2：能对话
### D6 ASR + 唤醒
- [ ] [ ]

下载模型：SenseVoice-Small(int8) + silero-vad + sherpa-onnx KWS 声学模型

- [ ] [ ]

`speech/kws.py`：注册唤醒词"小队长"，测试误触率（TV 环境音 30 分钟内误触<3 次）

- [ ] [ ]

`speech/asr.py`：VAD 切段→ASR→文本；延迟目标：说完→文本 ≤1s

- [ ] [ ]

验收：嘈杂环境（放视频背景音）唤醒+识别成功率 ≥90%

### D7 LLM 接入 + 说话权闸门
- [ ] [ ]

验收网关权限审批状态（未批→ollama+Qwen 先行，接口做成可切换）

- [ ] [ ]

`llm/client.py`：OpenAI 兼容接口；超时 3s；失败自动切本地

- [ ] [ ]

**说话权闸门 v1（安全核心）**：互动回答 = 工具查询结果 + 固定模板组装，LLM 只做槽位填充，禁止自由生成句子

- [ ] [ ]

验收：断网状态本地 LLM 兜底链路可出答案

### D8 值班表工具
- [ ] [ ]

向大队长要真实值班表结构；SQLite 建表：人员/时段/任务/状态/Backup

- [ ] [ ]

`tools/duty_roster.py`：按 MCP 形状实现 `list_duty / get_task / update_task / list_pending`

- [ ] [ ]

指标 mock：`tools/metrics.py` 返回随机游走的 mock 数据 + 阈值配置

- [ ] [ ]

验收：自然语言"支付组预案巡检到哪了"→ 工具调用 → 返回正确数据

### D9 遥控台 v1
- [ ] [ ]

`screens/console/`：播报队列（逐条 confirm 按钮）、直输框、模式开关、应急静音

- [ ] [ ]

identity 事件的"问候确认"卡片（感知模块联调用，先留 UI）

- [ ] [ ]

confirm 逻辑：confirm 级事件不点不放行

- [ ] [ ]

验收：手机浏览器打开遥控台，能遥控大屏播任意文本

### D10 W2 里程碑
- [ ] [ ]

完整对话闭环：唤醒 → "小队长，支付组预案巡检到哪了" → 工具查询 → 队长播报 → 任务卡打勾

- [ ] [ ]

3 分钟 demo（设计文档 §1.1）逐环节对表，跑通 ≥80%

- [ ] [ ]

全程录像存档

- [ ] [ ]

**里程碑标准：真人现场语音问 3 个问题，3 个都对**

---

## 依赖与红线提醒
| 事项 | 责任 | 截止 |
| --- | --- | --- |
| LLM 网关权限申请 | 存孝 | D0 |
| 指向性麦克风到货 | 工程 | W1 内 |
| 真实值班表数据 | 存孝找大队长 | D8 前 |
| 音色定版 | 存孝 | D3 |
| 台本话术 v1（开场/播报/兜底） | 存孝 | W2 中 |
| 人脸/声纹注册志愿者报名（W2.5 用） | 存孝 | W2 末 |
| 合规报备（感知模块红线清单） | 存孝 | W2.5 开工前 |


> 感知模块（人脸+声纹）排在 W2.5 时间盒，2-3 天，做不完整体砍，不占 W1/W2 资源。
>

